<UserControl x:Class="AcpUI.AcpListBoxMruFiles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:AcpUINS="clr-namespace:AcpUI"
             xmlns:AcpUIMruNS="clr-namespace:AcpUI.Mru"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
  <FrameworkElement.Resources>
    <XmlDataProvider x:Key="MruSrc" Document="{x:Static Member=AcpUIMruNS:MruFiles.MRUDoc}"/>
    <Style TargetType="{x:Type ListBox}">
      <Setter Property="Control.Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ListBox}">
            <Grid Width="Auto" Height="Auto">
              <Border x:Name="Border" CornerRadius="0,0,0,0"/>
              <ScrollViewer Focusable="False" IsTabStop="False" HorizontalScrollBarVisibility="Disabled">
                <StackPanel IsItemsHost="True"/>
              </ScrollViewer>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="Control.Background" Value="Transparent"/>
              </Trigger>
              <Trigger Property="ItemsControl.IsGrouping" Value="True">
                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="Control.BorderThickness" Value="0,0,0,0"/>
      <Setter Property="Control.Background" Value="Transparent"/>
    </Style>
  </FrameworkElement.Resources>
  <Grid DataContext="{Binding Source={StaticResource MruSrc}, XPath=/data/item}">
    <ListBox Name="MruListBox" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Hidden"
             ScrollViewer.VerticalScrollBarVisibility="Hidden" ItemsSource="{Binding}"
             IsEnabled="{Binding Path=IsEnabled, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type AcpUINS:AcpListBoxMruFiles}}}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Label>
            <Hyperlink IsEnabled="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsEnabled}">
              <StackPanel Orientation="Horizontal" Tag="{Binding XPath=@Path}">
                <TextBlock Background="Transparent" Text="{Binding XPath=@Index}"/>
                <TextBlock Background="Transparent" Margin="10,0,0,0" Text="{Binding XPath=@Name}"/>
              </StackPanel>
              <ToolTipService.ToolTip>
                <ToolTip>
                  <ContentControl.Content>
                    <TextBlock TextWrapping="Wrap" Text="{Binding XPath=@Path}"/>
                  </ContentControl.Content>
                </ToolTip>
              </ToolTipService.ToolTip>
            </Hyperlink>
          </Label>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>